# Micro-Agent Environment Variables
# Copy to .env and configure for your environment.
# The executable bootstrap resolves these values and wires supported providers;
# see docs/CONFIGURATION.md and TODO.md for unsupported integrations.

# Model configuration
MICRO_AGENT_MODEL_ENDPOINT=
MICRO_AGENT_MODEL_API_KEY=
MICRO_AGENT_MODEL_PROVIDER=
MICRO_AGENT_MODEL_CATALOG_ENDPOINT=
MICRO_AGENT_MODEL_CATALOG_TOKEN=

# Memory and session
MICRO_AGENT_MEMORY_ENDPOINT=
# Use sqlite:///... for single-process development or redis://.../0 (with the
# optional redis extra) for shared external sessions, memory, and idempotency.
MICRO_AGENT_SESSION_ENDPOINT=
MICRO_AGENT_IDEMPOTENCY_ENDPOINT=

# External policy resolution (required when definitions declare policy_refs)
MICRO_AGENT_POLICY_STORE_ENDPOINT=
MICRO_AGENT_POLICY_STORE_TOKEN=

# Downstream token exchange for remote MCP servers (optional)
# The exchange service receives the verified invocation identity and returns a
# short-lived Bearer token scoped to the MCP server audience.
MICRO_AGENT_TOKEN_EXCHANGE_ENDPOINT=
MICRO_AGENT_TOKEN_EXCHANGE_TOKEN=

# Logging
MICRO_AGENT_LOG_LEVEL=INFO

# Secret references in a definition (environment model_api_key_ref /
# security.credential_refs) are resolved from the environment variable
# named by the reference, e.g. MICRO_AGENT_MODEL_API_KEY above.
