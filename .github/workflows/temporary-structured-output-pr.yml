name: Temporary structured output bootstrap

on:
  pull_request:
    branches: [main]

jobs:
  marker:
    if: github.head_ref == 'feat/structured-output-20260902'
    runs-on: ubuntu-latest
    steps:
      - run: echo bootstrap
