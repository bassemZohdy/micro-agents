apiVersion: v1
kind: Secret
metadata:
  name: micro-agent-secrets
  labels:
    app: micro-agent
type: Opaque
stringData:
  # Template only. Do not commit a real value; use a secret manager or create
  # this Secret outside version control.
  MICRO_AGENT_MODEL_API_KEY: ""
